import React from "react";
import { Tabs, Collapse } from "antd";

const { TabPane } = Tabs;
const { Panel } = Collapse;
function Workout({}){
    return(
        <div></div>
    )
}
export default Workout;